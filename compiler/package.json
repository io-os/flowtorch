{
  "name": "flowtorch-compiler",
  "version": "0.0.1",
  "description": "the flowtorch compiler itself. currently the first official gen of it's kind.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "graham stachowiak (all rights reserved)",
  "license": "ISC",
  "dependencies": {
    "typescript": "^1.4.1",
    "typson": "^0.6.0"
  },
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-tsc": "^0.9.2",
    "tsd": "^0.5.7"
  }
}
